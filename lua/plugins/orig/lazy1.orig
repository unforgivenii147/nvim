return {
  {
    "folke/lazy.nvim",
    opts = {
      install = {
        clone = "git clone --depth 1 {{url}} {{path}}"
      },
      git = {
        clone = "git clone --depth 1 --filter=blob:none {{url}} {{path}}"
      }
    }
  }
}
